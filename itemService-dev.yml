spring:
  application:
    name: item-service

#server:
 # port: 8001

eureka:
  client:
    service-url:
      #eureka服务器的地址
      defaultZone: http://eureka1:2001/eureka,http://eureka2:2002/eureka
      #拉取注册信息间隔时间
    registry-fetch-interval-seconds: 30
#server:
#  port: 8001
---
spring:
  profiles: item1

server:
  port: 8001
---
spring:
  profiles: item2

server:
  port: 8002
